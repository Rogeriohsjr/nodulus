# Evidence: OpenCode development workflow

Baseline: merged main `22a8535`, public and globally installed Nodulus 2.0.0, Windows. OpenCode 1.18.32 and Ollama 0.32.15 are installed; `ollama list` contains `qwen3.5:9b`.

Planning probe, not acceptance proof: a project-local OpenCode configuration exposed `ollama/qwen3.5:9b`. `opencode run --format json --model ollama/qwen3.5:9b --agent plan` returned newline-delimited `step_start`, `text` and `step_finish` events; the final text was `READY`, and the finish event reported zero cost. This establishes the live local transport shape before implementation, not Nodulus adapter compatibility or file-editing behavior.

## Model-discovery correction (test-only RED checkpoint)

- Test-strengthening continuation: the fixture now records a single ordered invocation journal and refuses every OpenCode inference call unless the completed probe journal is exactly `['--version']`, then `['models', 'ollama']`. PROV-007 asserts that observable probe/probe/run order; this prevents a future adapter from satisfying independent probe assertions while starting inference too early.
- RED command: `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts`.
- Result: build passed; Vitest ran 16 tests, with 13 failures and 3 passes. The primary PROV-007 assertion received `['--version']`, then `['models', '--format', 'json']`, rather than the required exact model discovery probe. The run guard kept inference from starting. This is behavioral RED against the adapter's unsupported discovery command; production behavior remains unmodified pending test review.

- Live reconnaissance: OpenCode 1.18.32 rejects `opencode models --format json`; `opencode models ollama` exits 0 and prints newline-delimited full model IDs, including the exact `ollama/qwen3.5:9b`. The `opencode run --format json --model ollama/qwen3.5:9b --agent plan` stdin transport remains supported and returned the requested final text.
- Fixture correction: the real OpenCode fixture now rejects model discovery with `--format`, accepts only the argument array `models`, `ollama`, and emits full IDs one per line. Its available list includes the confusable `ollama/qwen3.5:9b-extended` alongside the exact configured ID, so the replacement adapter must use an exact full-line match rather than a substring.
- RED command: `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts`.
- Result: build passed; Vitest ran 16 tests, with 13 failures and 3 passes. The primary PROV-007 assertion observed `['models', '--format', 'json']` where the supported `['models', 'ollama']` argument array is required. The fixture rejects the former with `unknown option --format`; all inference-path variants consequently returned `PROVIDER_MODEL_UNAVAILABLE` before an OpenCode run invocation. This is a focused behavioral RED against the current adapter, not a harness failure.
- Corrected GREEN: `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts` passed on Windows after the adapter switched model readiness to `opencode models ollama`. All 16 PROV-007/PROV-008 tests passed. The success scenario observed version probe, exact model-discovery probe, then the stdin `run` call; the fixture list includes `ollama/qwen3.5:9b-extended`, confirming the configured `ollama/qwen3.5:9b` used full-line equality rather than a substring.
- Quality validation after corrected GREEN: `npm run lint` and `npm run typecheck` passed. `npm run check` passed its lint, typecheck and build stages, then ran 144 scenarios: 141 passed and three existing validator-timeout cases exceeded Vitest's five-second test timeout (`NODE-003 distinguishes validator timeout`; `SAFE-003 turns validator timeout into a runtime error with saved diagnostics`; `SAFE-003 cancels the production validator process on AbortSignal`). Package tests did not start because the scenario command failed. This is not a full quality-gate pass or live-provider proof.

## Test-only RED checkpoint

- Command: `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts`.
- Result: build passed; Vitest ran 16 tests and all 16 failed at behavioral assertions because the production default adapter rejects `kind: opencode` and returns `PROVIDER_FAILURE` rather than performing OpenCode readiness or inference.
- The focused scenarios keep `fixture-model` for Codex/Cursor and use `ollama/qwen3.5:9b` only for OpenCode. They cover the complete OpenCode model through discovery and run arguments; supported and unrecognized version failures before inference; safe captured profile fields without credentials or arguments; strict NDJSON extraction; a valid NDJSON response containing an invalid Nodulus outcome with exactly one writing invocation and `RESPONSE_REPAIR_UNAVAILABLE`; and raw transport evidence plus real-successor suppression for timeout, output limit, nonzero exit, malformed NDJSON (including its actual newline), and missing final text.
- This is RED only. No production adapter, intake, or user documentation behavior has been implemented in this checkpoint. `npm run lint` and `npm run typecheck` both passed after the test additions.

## Superseded fixture GREEN checkpoint

- Command: `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts`.
- Result: build passed; all 16 PROV-007/PROV-008 scenarios passed on Windows. This result remains evidence for the run transport, error handling and successor suppression, but its OpenCode model-discovery assertion used `models --format json`, which live OpenCode 1.18.32 rejects. It is not a corrected model-discovery GREEN.
- Quality commands: `npm run lint` and `npm run typecheck` passed. `git diff --check` passed.
- Limitation: `npm run check` reached the scenario suite but three unrelated validator-timeout tests exceeded Vitest's 5-second test limit locally (`NODE-003 distinguishes validator timeout`; `SAFE-003 turns validator timeout into a runtime error with saved diagnostics`; `SAFE-003 cancels the production validator process on AbortSignal`). The focused OpenCode suite passed independently; this is not hosted or live-provider proof.

The first implementation review found that timeout evidence discarded partial stdout/stderr and that delegated changes escaped the authorized scope. A corrective RED emitted identifiable partial output before timeout; GREEN preserves the unchanged process result. The out-of-scope core execution change was reverted. The normal-caller `npm run check` passed 38 scenario files / 144 tests and the seven installed-package tests. A separate model-sandbox/final-gate attempt exposed intermittent existing process-timeout tests; the immediate normal-caller rerun passed and no timeout threshold or unrelated test was changed.

DEV-003 RED: three development-workflow cases expected OpenCode/Qwen writer profiles, Luna test review with writable test execution, and GPT-5.6 Sol final review; all three failed against the earlier Luna/Sol configuration. A later two-case RED proved the final reviewer also needs workspace-write for build/Vitest generated files on Windows. GREEN gives both reviewers generated-file access while their instructions prohibit source edits, and retains the existing sequence, rejected-review stop and deterministic quality-gate scenarios.

At that checkpoint, installed-package and live-provider proof remained pending. The example profile now explicitly captures `responseRepair`; DEV-003 first failed all three cases when that capability was absent, then passed all three after the profile added it. This matters because adapter support alone does not authorize correction: the run snapshot must also opt in to the bounded, same-session, non-writing repair contract.

## Nested-event correction

- RED: `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts` built successfully and ran 17 tests with three parser-specific failures: normal nested final text and reasoning-only fallback returned exit 1, while a valid nested invalid outcome was incorrectly reported as `PROVIDER_RESPONSE_MISSING`.
- GREEN: the same command built successfully and passed all 17 tests. OpenCode now uses `--thinking`, identifies the final message by nested `step_finish.part.messageID` and `reason: "stop"`, prefers that message's nested text parts over reasoning, and falls back to reasoning only when text is absent. The fixture also proves an earlier nested text outcome ending in `tool-calls` is ignored.
- This remains local fixture proof, not a live-provider or cross-platform compatibility result.
- `npm run check` was run twice after GREEN. Lint, typecheck and build passed; both runs stopped before package tests when the existing validator-timeout cases (`NODE-003` and two `SAFE-003` cases) exceeded Vitest's 5-second test timeout. The scenario totals were 142 passed / 3 failed; no unrelated timeout behavior was changed.

## Complete-outcome transport suffix test-only RED

- Command: `npm run build`, then `npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts`.
- Result: build passed. Vitest ran 17 tests; 16 passed and the strengthened nested-final-message scenario failed because OpenCode stdin was exactly the complete persisted Nodulus prompt, with no adapter-owned transport suffix. The new assertion requires the untouched original prompt followed by two newlines and a suffix that requires one full Nodulus system outcome JSON response, rejects artifact `data` alone and writing the outcome to a file, and includes a concrete success shape containing `status`, `artifacts`, `name`, `contract`, and `data`.
- This is focused behavioral RED only. The next implementation step must append that suffix only to OpenCode stdin while retaining argument-array invocation, strict outcome/artifact validation, no replay, and existing Codex/Cursor behavior.

## Complete-outcome transport suffix GREEN

- `npm run build` passed, then `npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts` passed all 17 tests. The real OpenCode executable fixture observed the persisted Nodulus prompt unchanged before the exact adapter-owned suffix on stdin; the request remained out of the argument array.
- The OpenCode-only suffix requires the final assistant response to be one complete Nodulus system-outcome JSON object, explicitly rejects artifact `data` alone and file output, and includes the tested `status`/`artifacts`/`name`/`contract`/`data` success shape. It does not wrap or repair incomplete output; the existing core outcome and artifact validation still reject it without replay.

## Response-only repair GREEN

- The accepted strengthened RED had 17 passing and three intended repair failures in `tests/scenarios/prov-007-opencode-adapter.test.ts`: OpenCode lacked same-session response repair and therefore returned an unavailable-repair error.
- `npm run build; npx vitest run tests/scenarios/prov-007-opencode-adapter.test.ts` passed all 20 tests after the adapter added a response-only correction with the captured final-message session ID. The final live configuration uses the tool-denied `nodulus-response` agent because the built-in plan agent injected planning instructions during correction. The test fixture proves one build plus two response-only repairs where necessary, keeps `repair-001` and `repair-002` transports separate, and selects only final stopped-message text ahead of same-message reasoning and earlier-message distractors.
- A nonzero repair process saves its stdout/stderr in `repair-001/transport.json` and stops as `RESPONSE_REPAIR_FAILED`; it never starts another build action. This is local fixture proof only, not live OpenCode/Ollama or cross-platform proof.
- `npm run check` passed lint, typecheck and build, then stopped at 146 passed / 3 failed in the existing five-second validator-timeout tests (`NODE-003` and two `SAFE-003` cases). Package tests did not run, so full quality-gate and live-provider proof remain pending.

## Installed OpenCode workflow proof

- Local package proof used real archives installed globally as `2.1.0-dev.3`, `.4`, and finally `.5`; none was published. `nodulus doctor --json` reported the OpenCode builder, Luna test reviewer, and GPT-5.6 Sol final reviewer available. The public npm version remained `2.0.0`.
- Early installed runs were retained rather than rewritten: `70ee7ffe-bcbe-4e13-8ad2-db03d5dffd64` stopped when Luna found five cases and a nonexistent assertion API; `450cf900-7e43-4f13-b444-8c5f2a137dcc` and `ef6af32d-769b-4555-a013-d3a055274912` exhausted two response repairs; and `56e82255-0ce2-4466-8b39-5809a4db7fc0` stopped when Qwen implemented during the test-writing phase and Luna correctly rejected the missing RED checkpoint.
- Those failures drove two focused behavioral RED/GREEN changes. The response-repair prompt now repeats the exact nested success shape, and repair uses the project's `nodulus-response` primary agent rather than OpenCode's built-in plan agent. The example agent's final `*` permission denies every tool. `opencode agent list` confirmed that effective rule, and the six focused provider/workflow files passed 52 tests.
- Run `14ed5293-bd73-44b4-84dd-610e2d8b1729` used installed `2.1.0-dev.5`. OpenCode verified six assertion-based failures against the importable stub; the second response-only correction returned valid JSON. Luna independently reran the focused command and accepted the six-case RED checkpoint. The same run later stopped when its implementation response remained invalid, so it is evidence only for RED, repair, and Luna acceptance.
- In the retained correction run `56e82255-0ce2-4466-8b39-5809a4db7fc0`, OpenCode produced the finite-number guard and arithmetic implementation. Luna rejected that run because the edit occurred in the test-writing phase; the guard therefore prevented a false straight-through success. The resulting files independently passed all six focused tests.
- Installed verification run `2e440c75-de76-44c7-ab4a-47d594204caf` invoked GPT-5.6 Sol after the GREEN state. Sol inspected both files without editing, reran `node --test .nodulus/pilot-opencode-5/sum.test.mjs`, observed 6 passed / 0 failed, and returned ACCEPT. The fixed validator then ran the repository's actual `npm run check`; saved validation is `valid: true` with the `result` validator accepted.
- Final coordinator `npm run check` passed lint, typecheck, 38 runtime scenario files with 149 tests, and the installed-archive suite with 7 tests (156 total). No timeout threshold or quality rule was weakened.
- LIVE-001 is accepted from this retained multi-run correction chain, not from a claimed straight-through run. It proves Windows/OpenCode 1.18.32/Ollama 0.32.15 with local `qwen3.5:9b`, Luna, and GPT-5.6 Sol. macOS/Linux live OpenCode and live Cursor compatibility remain unverified.
