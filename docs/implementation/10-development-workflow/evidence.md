# Evidence: development workflow

Baseline: main `95182b1d24bff8899e690f3611552486df14a07c`; global public CLI upgraded from 1.0.0 to 1.0.1 and `nodulus --version` returned 1.0.1. Existing local `.nodulus/develop` has one implementation node and is preserved. Pilot work is in an isolated repository worktree.

DEV-001: configuration verification, no invented behavioral RED. The first run exposed the requirement that request mappings use built-in `request.v1`; correcting the example yielded 2/2 passing production-API scenarios with actual files, contracts and persistence. One verifies all four mappings, the other proves a rejected test review stops before implementation. Only inference is replaced.

Codex configuration verification (non-TDD): CLI 0.144.4 failed `codex login status` on the newer `[agents]` scalar defaults, with `invalid type: string ... expected struct AgentRoleToml`. Removing those unsupported entries while keeping top-level Luna/medium defaults made authentication probing succeed. This does not change the active desktop task's model or assert a usage cap. Node profiles carry explicit model/policy settings.

PROV-005 RED: five focused cases failed for missing sandbox argument, missing captured policies/default, and invalid values reaching the fixture provider. Build/lint/typecheck passed at this test-only checkpoint. GREEN and live results follow after review.

Live readiness exposed CLI-version and schema issues before any exercise files changed. The app-bundled CLI 0.144.4 rejected the Luna model; installing standalone `@openai/codex@0.156.1` and selecting its explicit executable path resolved that startup error. The newer CLI also accepts the project's original `[agents]` settings, so the temporary TOML compatibility edit was reverted; no project defaults were removed. On 0.156.1, run `1e739c54-f0da-4002-8443-619d9ab6a8dc` then returned `invalid_json_schema` because the adapter's `artifacts` array lacked an items schema. The failure is retained in the provider transport and is not a completed pilot. A strict transport regression and correction follow.

The Codex provider transport is being corrected to use a strict object with one required `response` string, containing the full Nodulus outcome JSON. This avoids pretending arbitrary user artifact schemas all satisfy the provider's strict JSON Schema subset. The raw envelope stays in provider `last-message.txt`; the unwrapped outcome is validated by the unchanged Nodulus protocol and artifact validators. A direct, no-tools Luna probe with this schema succeeded on CLI 0.156.1; that is transport/model availability proof only, not the four-node pilot.

PROV-005 GREEN: five focused policy tests and the original protocol file passed, with lint/typecheck/build. Sol accepted policy translation and captured resume behavior. PROV-006 RED: five failures reproduced the strict-schema/envelope incompatibility. GREEN: five focused cases passed, then Sol caught older shared fixtures returning bare outcomes. After updating all Codex fixtures (Cursor unchanged), the full protocol file passed 15/15 independently. The package fixture was updated to the same wire envelope.

DEV-002 live source pilot: `node dist/bin.js run --workflow develop-reviewed --request-file .nodulus/requests/pilot.md --json`, Windows, standalone Codex 0.156.1, run `3bd04658-52fb-4670-8b89-5eb6941deb75`. Luna/medium authored six real Node tests plus an importable stub; all six failed on behavioral assertions. Sol/medium independently reran RED and accepted. Luna implemented clamp; six focused tests passed. Sol independently ran the same six tests and accepted. Four validated node artifacts and terminal success were persisted. Models edited only the exercise files under `.nodulus/pilot/`; no product feature was implemented by this exercise.

Important limitation: the live implementation node also ran `npm run check`, which reported three 5000ms test timeouts (NODE-003 validator timeout; SAFE-003 validator timeout and cancellation). The reviewer accepted the focused exercise while explicitly retaining those failures. A coordinator run outside Codex passed lint/typecheck, 127 runtime/release tests and seven package tests. This does not prove that the full suite passes inside the Codex Windows sandbox.

The pilot exposed that reviewer acceptance alone must not define repository quality. DEV-001 was extended test-first: an actual temporary npm check script exits 7 while every provider says ACCEPT. Before the gate, workflow status was incorrectly success; strengthened tests also showed the execution marker was absent (2 failed / 1 passed). Sol accepted the strengthened RED. Added the final scripted npm-check validator and optional develop-verify workflow; all three cases then passed, proving real check execution, nonzero exit rejection and no check execution after early review rejection. No internal process or filesystem was mocked.

PROV-006 changes the Codex adapter's private wire schema to `{response:string}`. Custom executable wrappers used with `kind: codex` must honor that schema; normal Codex CLI follows the supplied output schema. Source profiles that relied on inherited write access now need explicit `sandbox: workspace-write`. These compatibility changes are called out in the PR's breaking-change title; the local preview uses `2.0.0-dev.0`, without changing the tracked source package version or publishing it.

DEV-002 installed preview verification: packed real archive and installed globally as `@rogeriohsjr/nodulus@2.0.0-dev.0`; `nodulus --version` returned that preview version. No preview was published. Using that installed CLI, `develop-verify` run `5ddddeba-29da-43ea-856a-1912f64afa01` invoked live Sol/medium read-only against the completed exercise; six focused tests passed and its artifact was ACCEPT. The configured runtime validator then ran the actual repository `npm run check` and accepted it; saved final validation has `validators: [{name: result, valid: true, errors: []}]` and terminal status success. This separately proves the installed adapter plus the final gate without replaying all four paid checkpoints. The four-node source pilot and one-node installed verification are distinct runs, not one claimed end-to-end installed run.

Final coordinator `npm run check`: lint/typecheck, 128 runtime/release tests and seven installed-package tests passed. The final gate also passed on the same source configuration. Full-suite execution inside the Codex Windows sandbox remains a documented limitation; no protection was bypassed and no test timeout was weakened. Remaining live checks: macOS/Linux provider invocation and deliberate live-provider rejection/clarification (fixtures cover those branches). Runtime usage summaries remain null; raw Codex transport includes vendor usage, but no billing or cost totals are claimed.
