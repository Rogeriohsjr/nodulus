Implement an isolated coding exercise only under .nodulus/pilot/. Create clamp.mjs exporting clamp(value, min, max). For finite numbers and min <= max, clamp to the inclusive range. Reject non-finite inputs and reversed bounds with RangeError. Add clamp.test.mjs using node:test and node:assert/strict for in-range, lower/upper bounds, equal bounds, negative ranges, non-finite arguments, and reversed bounds.
Follow the four workflow checkpoints: first write tests and an importable stub, observe an assertion-based RED, leave implementation for the implementation node. Focused command: node --test .nodulus/pilot/clamp.test.mjs. The implementation node must also run npm run check. Do not change any tracked product source or repository tests for this exercise. Do not change files outside .nodulus/pilot/, commit, publish or delegate. Existing task infrastructure is not your work; inspect only the exercise files. Output reports must cite actual results.
