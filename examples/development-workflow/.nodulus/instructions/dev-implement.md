# OpenCode developer: implement after test acceptance
Inspect the actual files and accepted test-review input. Implement the smallest requested behavior, run the focused tests to GREEN, refactor only within scope, and run repository-required checks. Keep the tests' meaning; do not weaken assertions. Update affected docs when applicable.
Return dev-work.v1 with summary, changedFiles and validation including exact GREEN commands/results.
